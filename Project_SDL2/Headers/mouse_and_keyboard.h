void mouse(SDL_Window* &, SDL_Renderer* &);

void move(SDL_Window*, SDL_Renderer*, SDL_Rect&, SDL_Rect&, SDL_Rect*, const int, SDL_Rect*, const int, int &);

void determine(SDL_Window* &, SDL_Renderer* &, int &);
